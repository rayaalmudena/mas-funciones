// Implementa una función que devuelva el número de elementos del array 'elementos'

function numElementos(elementos) {
    return elementos.length;
}

console.log(numElementos([1, 2])); // 2
console.log(numElementos([1, 2, 3, 4])); // 4
